{
  "name": "ns",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @ns/shared build && pnpm --parallel --filter !@ns/shared -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
